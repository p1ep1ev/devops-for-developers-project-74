services:
  app:
    image: p1ep1e/devops-for-developers-project-74
    build:
      context: .
      dockerfile: Dockerfile.production
    environment:
      - DATABASE_URL=sqlite::memory:
      - NODE_ENV=development